class Point {
    p;  //父节点
    x;  //x位置
    y;  //y位置
    g = 0;  //g值
    h = 0;  //h值
    g = 0;  //f值
    i = 0;  //节点唯一值
    constructor(p, x, y) {
        this.p = p;  //父节点
        this.x = x;  //x位置
        this.y = y;  //y位置
    }
}
export default Point;
