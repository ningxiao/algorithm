{
    "name": "a-star",
    "version": "0.0.1",
    "description": "A*寻路算法",
    "scripts": {
        "start": "http-server -c-1 -o index.html --proxy http://localhost:8080?",
        "test": "mocha"
    },
    "devDependencies": {
        "mocha": "^10.2.0"
    }
}
